<h2><%= @tag.name %></h2>
<table class="table">
    <thead>
        <tr>
            <th class="col">#</th>
            <th class="col">Author</th>
            <th class="col">Post title</th>
        </tr>
    </thead>
     <% @tag.taggings.each do |t| %>
        <tr>
            <td><%= t.id %></td>
            <td>
                <%# <%= link_to t.user.login, user_path(t.user), class: "link-info" %> %>
            </td>
            <td>
                <%# <%= link_to t.title, t %> %>
            </td>
        </tr>
        <%end %>
</table>