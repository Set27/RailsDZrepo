<%= simple_form_for @sub do |f| %>
    <%= f.association :user, label_method: :login, value_method: :id %>
    <%= f.input :subscribe_to, as: :select, collection: User.pluck(:login,:id)  %>   
    <%= f.button :submit, class: "btn btn-primary mt-2" %>
<%end%>