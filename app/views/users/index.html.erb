<h1>List of all Users</h1>

<ul class="list-group">
    <% @users.each do |user| %>
    <li class="list-group-item">
        <%= link_to user.login, user_path(user.id)%>
    </li>
    <% end %>
</ul>
<%= link_to "New", new_user_path, class: "btn btn-primary mt-2" %>