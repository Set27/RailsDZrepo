<h2>
    <%= @post.title %>
</h2>
<align="justify">
    <%= @post.body %>
        <p>
        <h2>Comments</h2>
        <% @post.comments.each do |comment| %>
            <%= link_to comment.user.email, user_path(@post.user) %>: <%= comment.body %><br>
                    <% end %>
                        <hr>
                        <%= link_to "New comment" , new_comment_path %>
                            <p>
                            <div style="text-align: right">
                                <b>Author:</b>
                                <%= link_to @post.user.email, user_path(@post.user) %>
                            </div>